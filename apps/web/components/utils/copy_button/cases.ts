import { TestCase } from '@/test/type'
import { CopyButtonProps } from '.'

export const Default: TestCase<CopyButtonProps> = {
  args: { text: 'text' }
}

// TODO: テストケースを追加してCasesに適宜追加する

export const Cases = { Default }
